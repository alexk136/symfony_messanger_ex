<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Messenger</title>
    <link href="//maxcdn.bootstrapcdn.com/bootstrap/4.1.1/css/bootstrap.min.css" rel="stylesheet" id="bootstrap-css">
    <script src="//maxcdn.bootstrapcdn.com/bootstrap/4.1.1/js/bootstrap.min.js"></script>
    <script src="//cdnjs.cloudflare.com/ajax/libs/jquery/3.2.1/jquery.min.js"></script>
    <style>
        body {
            background: #f8f9fa;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
        }
        .home-container {
            background: white;
            padding: 3rem;
            border-radius: 0.5rem;
            box-shadow: 0 0 10px rgba(0,0,0,0.1);
            text-align: center;
            width: 100%;
            max-width: 500px;
        }
        .btn-custom {
            margin: 0.5rem;
        }
    </style>
</head>
<body>
    <div class="home-container">
        <h1 class="mb-4">Welcome to Messenger</h1>
        <div>
            <a href="{{ path('app_login') }}" class="btn btn-info btn-lg btn-custom">Пользователь</a>
            <a href="{{ path('app_login_operator') }}" class="btn btn-warning btn-lg btn-custom">Оператор</a>
        </div>
    </div>
</body>
</html>